{
  "Class": {
    "prefix": "sc",
    "body": [
      "Class {",
      "  constructor():void {",
      "  }",
      "}"
    ],
    "description": "Class"
  },
  "競技プログラミング用初期化コード": {
    "prefix": "CompetitiveProgrammingInit",
    "body": [
      "var reader = require('readline').createInterface({",
      "  input: process.stdin,",
      "  output: process.stdout",
      "});",
      "",
      "reader.on('line', (line: string) => {",
      "  lines.push(line);",
      "});",
      "",
      "reader.on('close', () => {",
      "  var N = Number(lines[0]);",
      "  for (var i = 0; i < N; i++) {",
      "    var line = lines[i + 1].split(\" \");",
      "    console.log(\"hello = \" + line[0] + \", world = \" + line[1]);",
      "  }",
      "});"
    ],
    "description": "競技プログラミング用初期化コード"
  }
}
